<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DistanceConverter"
             xmlns:views="clr-namespace:DistanceConverter.Views"
             x:Class="DistanceConverter.App">
    
   <!-- <TabBar x:Name="tbBar">
        <Tab Title="Distance" Icon="distance_tab">
            <ShellContent ContentTemplate="{DataTemplate views:DistancePage}" Route="DistancePage" />
        </Tab>
        <Tab Title="Distance" Icon="weight_tab">
            <ShellContent ContentTemplate="{DataTemplate views:WeightPage}" Route="WeightPage" />
        </Tab>
    </TabBar> -->

    <Application.Resources>
    
        <ResourceDictionary>
            
        </ResourceDictionary>
    </Application.Resources>
</Application>
